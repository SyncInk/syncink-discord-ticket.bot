import React from 'react';

const Settings = () => {
  return (
    <div className="card animate-slide-up">
      <h2>Bot Configuration</h2>
      <p style={{ color: 'var(--text-muted)', marginTop: '8px' }}>Manage global bot settings here.</p>
    </div>
  );
};

export default Settings;
