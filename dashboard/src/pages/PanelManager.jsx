import React from 'react';

const PanelManager = () => {
  return (
    <div className="card animate-slide-up">
      <h2>Ticket Panel Management</h2>
      <p style={{ color: 'var(--text-muted)', marginTop: '8px' }}>Build and preview Discord ticket panels here.</p>
    </div>
  );
};

export default PanelManager;
